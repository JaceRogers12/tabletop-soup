import React from "react";

function Profile() { // the profile page needs actual content
    return(
        <div className="profile content" >
            <p>The profile route works!</p>
        </div>
    );
};

export default Profile;